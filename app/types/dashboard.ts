export type MSR = {
  id: number;
  name: string;
  year: number;
  created_by: string;
  created_at: string;
};

export type Category= {
    id: number,
    name: string
}

export type Proto = {
  id: number;
  name: string;
  qr_number: string;
  status: string;
};